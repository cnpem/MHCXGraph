# TODO: add instructions to run minimal example

python3 -m immunograph.app --manifest manifest.json

> Running from the base directory, the manifest file is located at `examples/minimal/manifest.json`, so the command should be:

```bash
MHCXGraph examples/minimal/manifest.json
```

<!-- run in base directory -->
